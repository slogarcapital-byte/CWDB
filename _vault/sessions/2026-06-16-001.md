---
type: session
session-id: "2026-06-16-001"
agents-used: []
topics: [estimator, qa-protocol, ios-safari, ai-renderings, exif-orientation, streamlit-secrets, deployment, pdf-formatting]
decisions-made: [exif-transpose-orientation-fix, gemini-full-material-prompt, mockup-below-scope, ios-safari-input-css, deploy-test-branch-ff-main, gemini-key-in-cloud-dashboard]
files-changed: [sales/estimating/streamlit_app/render_mockup.py, sales/estimates/generate_estimate_pdf.py, sales/estimating/streamlit_app/app.py]
previous-session: "[[2026-06-15-001]]"
next-session: "[[2026-06-17-001]]"
duration-minutes: 0
tags:
  - type/session
  - session/2026-06
created: 2026-06-16
updated: 2026-06-16
status: in-progress
warehouse-last-run: "2026-06-16T11:55:30Z"
warehouse-last-status: "success"
env-vars-missing: 0
---

# Session 2026-06-16-001

## Context

Continuation of the 2026-06-15 estimator feature build ([[2026-06-15-001]]). Five threads, in order: (1) walk Jim through the go-live steps (Gemini key, redeploy, John pricing); (2) develop + run a QA protocol on the whole feature build before deploy; (3) commit + deploy the feature to production; (4) three live-feedback tweaks after Jim tested an estimate end-to-end from his iPhone; (5) fix an iOS-Safari "white-on-white" form-field regression that blocked further testing. Session spans the 06-16 -> 06-17 date boundary.

## Work Done

- **QA protocol (skill: `verify`).** Multi-surface verification of the 2026-06-15 build, all on the real surfaces:
  - Streamlit UI driven live via Playwright (Chromium): default deck, project-type switch to Fence (panel swap + $/LF unit + fence-aware send gating), New Build, decking->color cascade (Trex Select/"Pebble Grey" -> AZEK Vintage/"Coastline"), live price recompute, send-gating enable. 0 console errors.
  - PDF generation for deck-with-renderings, fence, new-build via the same `build_estimate_json -> generate_pdf` path the app uses; verified Design Mock-Up section, "COMPUTER-GENERATED SIMULATION" disclaimer, color-aware labels, ATCP subordination clause, fence line items; line items reconcile to sell price to the dollar.
  - Excel: `test_workbook.py` (10 deck scenarios via real `formulas` evaluation) + a fresh Python<->Excel parity check on the new types. **Parity to the dollar: New Build $44,148 == $44,148; Fence $8,300 == $8,300.**
  - Watermark image renders the legal "AI SIMULATION - ILLUSTRATION ONLY" banner; renderer fails open (returns []) on all 4 failure paths (no key / None key / no photo / bad path).
  - Em-dash scan: new scope files + fence/new-build PDFs clean; one PRE-EXISTING em-dash in `scope-copy/scope-build.md:55` (Full Tear-Out + Frame Rebuild PDFs), flagged not fixed (out of scope, not a regression).
- **Deploy plumbing.** Diagnosed Codespace vs Streamlit Community Cloud secrets; corrected Jim's key placement (real key belongs in the gitignored `.streamlit/secrets.toml`, NOT the committed `secrets.toml.example`); established that Streamlit resolves the project secrets file relative to the shell CWD, so the app must be launched from `sales/estimating/streamlit_app/`.
- **Shipped the feature (commit `c61f042`).** Staged exactly the 12 estimator files; merged `origin/main` to absorb the control plane's 2 "routing go-live" commits (clean, no estimator overlap), then fast-forwarded `main` (`26d6fca`). Never force-pushed; the autonomous control-plane commits preserved.
- **Three mock-up fixes after Jim's iPhone test (commit `d3513e8`)**, root-caused from the actual Johnson PDF (skill: `systematic-debugging`):
  - #2 orientation: renders were sideways because iPhone EXIF orientation was ignored. Fixed with `ImageOps.exif_transpose()` in `render_mockup._open_image` (NOT a blind 90deg rotate, which would break landscape shots). Proven on a synthetic orientation-6 image: old `convert()` kept (300,100); new `_open_image` returns (100,300).
  - #3 detail render: Gemini was dropping a corner material *swatch* instead of rendering the deck, because the prompt said "emphasizes the material texture." Rewrote both prompts to apply the material to the ENTIRE structure and explicitly forbid swatches/sample insets.
  - #1: moved the Design Mock-Up PDF section below Scope of Work (verified via pypdf text order).
- **iOS Safari readable-fields fix (commit `09bc5ff`).** Form fields rendered white-on-white on iPhone (Safari paints native/autofilled inputs with a light background that plain `background` CSS can't override). Added `-webkit-appearance:none`, opaque dark `background-color`, `-webkit-text-fill-color`, and a `:-webkit-autofill` `box-shadow` inset override. Verified non-regression on Chromium; Jim confirmed readable on-device.
- **On-device confirmation (Jim):** fields readable, and both renders came in with the selected material.

## Decisions Made

- **Deploy model:** work on `test-branch`, merge `origin/main`, fast-forward `main`. `origin/main` had diverged (2 autonomous control-plane routing commits); preserved them via merge rather than force-push. Pushed both branches each cycle so whichever branch Streamlit Cloud watches is covered.
- **Orientation = EXIF transpose, not blind rotate** (respects each photo's true orientation; safe for portrait and landscape).
- **Gemini prompt: full-structure material treatment on both shots; swatches/insets explicitly forbidden** (root cause of the "close-up didn't take").
- **Design Mock-Up section sits below Scope of Work** in the PDF.
- **iOS Safari dark-theme inputs require webkit overrides** (`-webkit-text-fill-color`, autofill `box-shadow`, `appearance:none`).
- **Gemini API key lives in the Streamlit Cloud dashboard secrets** (not a repo file) for the deployed app; paid tier per legal.

## Open Items

- Confirm render **orientation is upright** on the latest PDF (Jim confirmed material on both renders; upright not yet explicitly confirmed). If still rotated, move orientation handling to the renderer OUTPUT stage.
- **Confirm TimberTech / fence / gate pricing with John** (all `CONFIRM WITH JOHN` placeholders; math verified, inputs are his to sign off).
- Verify the `AQ.Ab8...` Gemini key is a standard API key, not an ephemeral/OAuth token (unusual prefix; renderer fails open silently if wrong).
- Add "renderings/visualizations" to the combined work-order **merger clause** (binding-contract text -> legal).
- One-line **privacy-policy** note on the site re: third-party AI photo processing.
- Pre-existing em-dash in `scope-copy/scope-build.md:55` (standing-rule violation, customer-facing; one-char fix offered, not applied).

## Agent Activity

- No specialty subagents dispatched; all work done in the main session.
- Skills used: `verify` (QA protocol framework), `systematic-debugging` (root-causing orientation + swatch + iOS issues from the real artifact before fixing), `session-end`.
- Playwright MCP (Chromium) drove the live local Streamlit app for UI verification.

## Memory Updates

- Two reusable platform gotchas captured to project memory: [[ios-safari-streamlit-dark-inputs]] and [[streamlit-secrets-location]].
- Estimator features now **DEPLOYED LIVE** on `main` + Streamlit Cloud (2026-06-17): TimberTech decking/railing, color selection, New Build + Fence types, AI mock-up renderings. Renderer needs the paid Gemini key in the Cloud dashboard secrets; iPhone EXIF handled via `exif_transpose`. Updated [[estimator-app-live]] pointer in MEMORY.md.

## Next Session

- Get John's confirmed pricing -> replace `CONFIRM WITH JOHN` placeholders and representative TimberTech colors with verified lists.
- Close the two legal follow-ups (merger clause + privacy line).
- If any render still comes back rotated, add output-stage orientation handling in `render_mockup`.

