---
type: session
session-id: "2026-07-06-001"
agents-used:
  - Explore
  - general-purpose (counsel pipeline, headless)
topics:
  - cwdb-hq-dashboard-build
  - consent-gate-fix
  - v-pl-monthly-rebuild
  - board-of-counselors
  - cloud-twin-rls
decisions-made:
  - dashboard-tasks-canonical-board-mirrors
  - event-log-plus-dashboard-sync-loop
  - counsel-5-lenses-plus-chairman
  - cloud-twin-anon-key-only
files-changed:
  - operations/dashboard/ (new app: app.py, tabs/, lib/, launch-dashboard.ps1, run-counsel.ps1, README.md)
  - operations/data-warehouse/schema/013_dashboard_hq.sql
  - operations/data-warehouse/schema/014_seed_audit_2026_07_05.sql
  - operations/data-warehouse/views/007_construction_kpis.sql
  - operations/data-warehouse/views/008_cloud_twin_read_path.sql
  - operations/data-warehouse/views/009_clean_leads_safe.sql
  - templates/scripts/pull-hubspot-snapshot.ps1
  - templates/scripts/pull-qbo-financials.ps1
  - templates/scripts/session-start.ps1
  - .claude/skills/dashboard-sync.md
  - .claude/skills/counsel.md
  - .claude/skills/session-end.md
  - _vault/board/*.md (now GENERATED mirrors)
  - CLAUDE.md
previous-session: "[[2026-07-05-003]]"
next-session: "[[2026-07-06-002]]"
duration-minutes: 90
tags:
  - type/session
  - session/2026-07
created: 2026-07-06
updated: 2026-07-06
status: in-progress
warehouse-last-run: "2026-07-06T05:18:45Z"
warehouse-last-status: "success"
env-vars-missing: 0
---

# Session 2026-07-06-001

## Context
Jim commissioned the CWDB HQ dashboard from the 2026-07-05 audit: 4 tabs (KPIs/exec/strategy, owner-grouped to-do with action buttons, platform diagnostics, QBO P&L), refreshed on command, with a two-way loop between dashboard actions and Claude's memory/agents/files/APIs. Design settled through a 9-question interview (see plan file `~/.claude/plans/brainstorm-a-dashboard-for-velvet-dragonfly.md`).

## Work Done
- **Phase 1 (instruments stop lying):** schema 013 dropped the `CHECK (tcpa_consent_given = true)` constraint (root cause of the 3 dropped leads) and added `consent_missing` + `hs_analytics_source` to fact_leads; HubSpot pull now ingests-and-flags instead of skipping (with an ALERT log line), stops wiping `lead_score`, and applies earliest-wins to `accepted_at` (HubSpot closedate re-stamped bid 4 to 7/6; corrected to true 6/11). Petersen/Hanson/Neely backfilled (Petersen shows PAID_SOCIAL). `v_pl_monthly` rebuilt: phantom $1,000 fee dead, QBO construction leg added (June: $3,605 revenue vs $1,179 spend = +$2,426). 6 new construction-era KPI views. New `pull-qbo-financials.ps1` fills `fin_pl_monthly`/`fin_position`/`fin_job_profit` (verified to the cent vs audit: cash $2,952.04, YTD net +$1,550.35).
- **Phase 2 (app):** Streamlit app at `operations/dashboard/` (4 tabs, brand-styled, port 8511, on-demand launcher). All tabs verified rendering via Playwright with live data. 28 audit fixes seeded into `dashboard_tasks` (4 pre-done), 18 `audit_findings` rows linked to fix tasks.
- **Phase 3 (two-way loop):** every dashboard action appends `dashboard_events`; `session-start.ps1` surfaces unprocessed events (ACTION REQUIRED + queued tasks); new `/dashboard-sync` skill ingests with judgment; `/session-end` now emits a `session_summary` event. Board files regenerated as read-only mirrors (old content safe in commit 99dfd76).
- **Phase 4 (counsel):** `/counsel` skill (specialty agents → CEO brief → 5 control-plane lenses re-aimed at construction goals → independent chairman) + `run-counsel.ps1` headless runner. Debugged three headless gotchas: npm .ps1 shim not executable by Start-Process (use .cmd), skill slash-commands not resolved in `-p` mode (load skill file via prompt), and Start-Process argv truncation through the .cmd shim (prompt now passed via stdin).
- **Phase 5 (cloud twin):** RLS read path (views/008-009): anon key reads dashboard tables + aggregate KPI views only; closed a PRE-EXISTING PII hole (v_clean_leads was definer-rights, exposing fact_leads PII to anon-key holders); new non-PII `v_clean_leads_safe` feeds the twin-readable aggregates; `v_kpi_cycle_time` no longer exposes names. Verified via curl: aggregates readable, PII blocked, writes are RLS no-ops. Deploy runbook in `operations/dashboard/README.md`.

## Decisions Made
- `dashboard_tasks` is the canonical task tracker; board .md files are generated mirrors (never hand-edit).
- Memory feedback is event-log + Claude-ingests (/dashboard-sync), not direct writes.
- Board of counselors: keep all 5 control-plane lenses + independent chairman; agents feed CEO, CEO feeds board.
- Cloud twin: Streamlit Cloud (test-branch, estimator pattern), anon key + RLS only, passcode-gated.
- Test-exclusion predicate now intentionally lives in BOTH v_clean_leads and v_clean_leads_safe (documented in views/009).

## Open Items
- First full counsel run was in progress at session end (counsel_runs row; Tab 1 renders it when complete).
- Streamlit Cloud twin deploy is manual (share.streamlit.io + secrets per README); iPhone Safari acceptance is Jim's.
- Known wrinkle: each headless counsel run triggers the SessionStart hook and creates a stub session note (2026-07-06-002..006 today are stubs from testing).
- Audit tasks #5 (consent re-capture), #27 (test lead 5) etc. now live in the dashboard to-do tab.

## Agent Activity
- Explore agent mapped Mission Control/warehouse/QBO/estimator/board infrastructure pre-design.
- Headless counsel runs launch: 4-5 specialty agents + CEO + 5 lenses + chairman per convening.

## Memory Updates
- New memory file `cwdb-hq-dashboard.md` + MEMORY.md standing rule, key-decisions entry, and detail-file pointer.
- CWDB/CLAUDE.md: Validation Gate section replaced with Phase 2 verdict; Operating Rhythm rewritten around the dashboard + weekly review.

## Next Session
- Check the counsel run result on Tab 1; adopt/reject recommended moves.
- Deploy the cloud twin (README steps) and verify on iPhone Safari.
- Work the P0s from the to-do tab (insurance call, DSPS clock, lead follow-ups).
