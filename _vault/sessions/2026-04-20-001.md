---
type: session
session-id: "2026-04-20-001"
agents-used: ["web-dev"]
topics: ["Phase 2 completion", "Phase 2.5 Part A", "process-steps-v2", "gallery-featured", "builders-strip", "coverage-map", "cta-final", "faq-section-home", "hero-interior", "homepage assembly", "Webflow MCP gotchas", "parallel agent orchestration", "manual-actions staging"]
decisions-made:
  - "Proceed with Phase 2 homepage build despite Wausau CMS binding issue — Phase 2.5 refactor fixes all 5 cities at once"
  - "Hardcode 3 gallery-featured cards from agent-chosen CMS items (cedar pergola Merrill, composite Wausau, multi-level Weston) rather than CMS-repeat binding for v1"
  - "Ship coverage-map dot-and-line constellation fallback as planned; custom SVG deferred post-launch"
  - "Build new `faq-section-home` component vs reusing existing 5-slot-static `faq-section` — agent judgment call on cleaner outcome"
  - "Hardcode 6 FAQs from CMS by sort-order (no featured-on-homepage field exists) with see-all link to /faq"
  - "About hero-interior shipped as inline section (not component instance) — MCP couldn't promote it; optional Jim-manual upgrade"
  - "Phase 2.5 Part B blocked pending Jim-manual CMS binding + Privacy/Terms hero treatment decision (no autonomous path forward)"
  - "Maintain separate manual-actions staging doc instead of interrupting autonomous flow each time a Jim action is discovered"
files-changed:
  - "_vault/sessions/2026-04-20-001.md (this file — populated)"
  - "_vault/jim-manual-actions-2026-04-20.md (created + appended throughout session)"
  - ".claude/projects/.../memory/MEMORY.md (3 new memory entries linked)"
  - ".claude/projects/.../memory/webflow-mcp-pseudo-elements.md (created, later expanded with attribute-selector + inline-script encoding findings)"
  - ".claude/projects/.../memory/webflow-mcp-parallel-agents.md (created — Designer page context is shared state)"
  - ".claude/projects/.../memory/webflow-mcp-sibling-insert.md (created — can't insert siblings adjacent to body-level component instances)"
  - "Webflow (source of truth): 7 new components built — process-steps-v2, gallery-featured, builders-strip, coverage-map, cta-final, hero-interior, faq-section-home"
  - "Webflow: homepage body assembled/cleaned — 13 deprecated children removed from main-sections, wrapper deleted, spine reordered"
  - "Webflow: hero-interior placed on city template (propagated to all 5 city pages) + About page"
previous-session: "[[2026-04-19-003]]"
next-session: "[[2026-04-20-002]]"
duration-minutes: 240
tags:
  - type/session
  - session/2026-04
created: 2026-04-20
updated: 2026-04-20
status: complete
---

# Session 2026-04-20-001

## Context

Jim opened the session with "let's pick up where we left off yesterday with web dev." The 21-page site revamp was blocked on 9 manual Webflow Designer toggles that only Jim could do. Once Jim completed those and published staging, the session turned into a long autonomous run: Jim instructed "get as far as you can without manual intervention, stage all of my manual tests and wait for them at the end unless they become legitimate blockers." From that point, the CEO/orchestration layer dispatched web-dev subagents sequentially and in parallel to ship Phase 2 Steps 6–11 + Step 11.5 + Phase 2.5 Part A, with a running manual-actions staging doc for anything that required Jim's physical touch.

## Work Done

Long autonomous web-dev run. Started with Jim clearing the 9 manual Webflow Designer toggles that were yesterday's blocker, then shifted into "get as far as you can without manual intervention" mode: 7 web-dev subagent dispatches (2 rounds of 3-way and 2-way parallel, then sequential for assembly), 7 new Webflow components built + placed, homepage body cleaned of 13 deprecated sections, full spine reordered to match the approved target, FAQ section backfilled after it surfaced as missing, hero-interior component shipped across 5 city pages + About. Three new MCP gotchas discovered and memoized. Phase 2.5 Part B hit a clean stop at Jim-manual dependencies (Component Properties creation + CMS bindings not exposed via MCP on single-locale sites) — staged everything in `_vault/jim-manual-actions-2026-04-20.md` for Jim to pick up. Detailed breakdown by workstream below.

### 1. Blocker unblocked: 9 manual Webflow toggles

- Jim asked for the exact checklist; pulled it from prior session context + reconstructed the 9-click sequence (Visibility=false on embedded hero inside `header` component on 7 pages: Home, Get a Quote, Thank You, FAQ, Our Builders, Gallery, Cost Calculator; Title overrides on Privacy + Terms)
- Jim executed and published to staging: "toggles done and published to stage."

### 2. Verification snapshot (gating Phase 2 Step 6)

- Dispatched web-dev to verify 6 pages via Playwright DOM
- Home / Get a Quote / Privacy: PASS clean
- Terms: title override landed but empty-H1 debris + wrong SEO title — P1 cleanup queued
- About: embedded hero intentionally retained (unchanged) — plus a placeholder "Heading 1" in rich-text body — P2 cleanup queued
- Wausau city: FAIL — CMS binding on embedded hero not firing; shows generic homepage fallback text
- Decision: Wausau/city CMS issue is Phase 2.5's job (refactor fixes all 5 at once). Homepage build proceeds.

### 3. Phase 2 Step 6 — `process-steps-v2` built

- Component `08624456-cfb4-450f-f121-c4e6c4f174a9`; homepage instance `83e5fb64-a576-a683-692b-52433dd2183e`
- Dark slate section, 3-column grid, massive Staatliches numerals 01/02/03 in orange, white titles, connector line
- Connector: whtml_builder silently stripped `::before` pseudo → agent fell back to absolutely-positioned `aria-hidden` div. **First MCP memory saved:** `whtml_builder` strips pseudo-elements.
- Placed before `main-sections`, after `hero-split`
- Verified desktop + mobile on staging

### 4. Phase 2 Steps 8/9/10 in parallel

Dispatched 3 web-dev agents concurrently with non-overlapping placement anchors:

- **Step 8 `builders-strip`** — Component `7e090a7f-fa01-b93b-fe8f-d3f17f6de3ed`, instance `007c6213-cdb8-a49c-f7bf-a78d27770633`. Slate band with 40% black overlay (#1e2020), orange eyebrow, 2 contractor rows (Ben Barton + John Garcia), sky-blue "Meet the team →" link. Headshots loaded from Webflow CDN. Agent used real `aria-hidden` overlay div pre-emptively (memory paying off).
- **Step 9 `coverage-map`** — Component `17113d3e-c5b2-2195-43c6-6322e0e21cdc`, instance `38751acc-393c-9590-0ce2-5c8ca289ba64`. Dot-and-line constellation SVG (5 sky dots + 4 lines), city list band right column, Staatliches 32px uppercase links to `/service-area/<city>`. Parked before `mobile-sticky-bar` (interim).
- **Step 10 `cta-final`** — Component `89566b0c-08a5-612a-25f7-1452a65d719d`, instance `122cc21c-2f5b-db06-2ab8-d4b8f21db27c`. Full-width slate band, 128/80 padding, Staatliches 56/36 uppercase headline, single orange CTA → `/get-a-quote`. Placed before `footer`. **Flagged:** Component Properties (headline, cta_label, cta_href) can't be created via MCP — queued Jim-manual.

### 5. Phase 2 Step 7 + Phase 2.5 Part A in parallel

- **Step 7 `gallery-featured`** — Component `182ba5d8-1b29-7e4d-30f1-99ff54130c65`, instance `4836d90b-9938-3bdb-d77d-b529ebad39ae`. Agent queried Gallery Photos CMS, picked 3 items spanning product range: cedar pergola Merrill / composite Wausau / multi-level Weston. Hardcoded (not CMS-bound) for v1. 16:10 photos, Staatliches project-type labels, hover opacity shift via inline site script workaround. **Flagged:** the 3 CMS items have placeholder images (interior living room, hotel bedroom, tool close-up) instead of real decks — blocks ad launch.
- **Phase 2.5 Part A `hero-interior`** — Component `ec0fc950-6cea-2755-d848-50ade4f2af3c`. Net-new interior-page hero: eyebrow / title / subtitle / optional bg_image. Inserted once at city template level → propagated to all 5 city pages; also placed on About. City pages currently show placeholder title "DECK BUILDERS IN YOUR CITY" because Component Properties + CMS bindings must be Jim-manual (two MCP limitations: Component Properties can't be created via MCP, and per-instance CMS binding isn't exposed on single-locale sites). About landed as inline section (not component) because MCP couldn't promote it.

**Parallel-agent page-context conflict discovered:** the Step 7 agent's first `transform_element_to_component` failed because the Phase 2.5 agent had switched Designer to Service Areas Template just before. Agent recovered by re-selecting home. **Second MCP memory saved:** Designer page context is shared state across parallel agents.

### 6. Phase 2 Step 11 — homepage assembly + cleanup

- Dispatched web-dev to inventory + clean `main-sections` wrapper
- Found 13 deprecated children inside: 4× `cedar-strip` dividers, `horizontal-cards`, `how-it-works` (old process), `three-up` value props, `accent-calculator`, `gallery` (old "Recent Deck Projects"), `coverage-area-cards` (old 5-city grid), `quote-form-inline`, `blog` teaser
- All 13 removed in batches; `main-sections` wrapper itself deleted
- Parked `coverage-map` moved from end-of-body to correct spine position
- **New MCP constraint discovered:** body-level sibling insert adjacent to component instances fails with "Cannot insert elements directly into a component instance." Agent's workaround: remove + re-append trailing 4 sections in correct final order. **Third MCP memory saved.**
- **Gap discovered:** FAQ was never on the homepage. Target spine requires it between coverage-map and cta-final, but no `faq-section` existed. Triggered Step 11.5.

### 7. Phase 2 Step 11.5 — FAQ section added

- Dispatched web-dev to add FAQ to homepage
- Agent inspected existing `faq-section` component (used on /faq page) — 5-slot static cap, no CMS binding — judged Path B cleaner: build new homepage-scoped component
- **New component `faq-section-home`** — Component `89e82a46-a8d3-cf05-801c-8ab696580bcb`, instance `e15715de-e3fd-e68c-513a-9e0ca86a67fb`
- 6 FAQs hardcoded (first by sort-order) + "See all frequently asked questions →" link to /faq
- Accordion chevron rotation: whtml_builder stripped `[open]` attribute selector + HTML-encoded inline `<script>` quotes → agent registered `faqhometoggle` as footer-scoped site script via `data_scripts_tool.add_inline_site_script` + `upsert_page_script`. Working via Playwright. **Memory updated with two additional whtml_builder gotchas** (attribute selectors + inline script encoding).
- Body-level `before` insertion worked this session (Step 11 constraint didn't reproduce — possibly state-dependent)
- Final spine verified live: `header → hero-split → process-steps-v2 → gallery-featured → builders-strip → coverage-map → faq-section-home → cta-final → footer → mobile-sticky-bar` — matches approved target exactly

### 8. Manual-actions staging doc maintained throughout

Created `_vault/jim-manual-actions-2026-04-20.md` at the start of the autonomous run and appended findings from every agent. Final doc has 9 numbered Jim-manual items grouped P0/P1/P2/P3 with exact click paths. Jim left inline `#jim ...` notes mid-session indicating: (a) he created the `bg_image` Component Property on `hero-interior` but discovered there's no image component under the hero-interior_bg to bind to — structural follow-up needed; (b) he chose Option A for Privacy/Terms and reused `hero-interior_about` which he created; (c) gallery photos should come from `website/pages/gallery/project-photos`; (d) he'll do social URLs + Nextdoor verification later.

## Decisions Made

1. **Proceed with Phase 2 homepage build despite Wausau CMS binding issue.** Phase 2.5 refactor is the comprehensive fix — patching Wausau alone would be redundant work.
2. **Ship non-CMS-bound gallery-featured for v1** — 3 hardcoded cards pulled from agent-chosen CMS items. Faster; Jim edits via asset replacement.
3. **Ship coverage-map constellation fallback** — custom Marathon/Lincoln county SVG deferred post-launch (matches 2026-04-19 decision).
4. **Build `faq-section-home` new instead of reusing the existing 5-slot static `faq-section`.** Reuse would have required duplicating every brand-design touch; building was cleaner.
5. **Hardcode 6 FAQs by sort-order** since no `featured-on-homepage` boolean exists on the FAQs collection. /faq remains authoritative.
6. **About hero-interior shipped as inline section (not component instance)** — optional Jim-manual upgrade via "Create Component from Selection" in Designer.
7. **Phase 2.5 Part B blocked on Jim-manual.** No autonomous path because: Component Properties can't be created via MCP (cta-final + hero-interior both need this), per-instance CMS binding isn't exposed on single-locale sites, and Privacy/Terms need a hero treatment decision before the embedded-hero nuke can run cleanly.
8. **Maintain a separate manual-actions staging doc** instead of interrupting the autonomous flow every time a Jim-only action was discovered. All findings appended in one place with exact click paths.

## Open Items

### P0 — blockers for Phase 2.5 Part B (Jim-manual)
- [~] Create Component Properties on `hero-interior` + bind city template instance to Service Areas CMS (Jim has started — created `bg_image` property but flagged: no image component under hero-interior__bg to bind to; structural fix needed in a follow-up micro-task)
- [x] Privacy/Terms hero treatment decision — **Jim chose Option A** and reused `hero-interior_about` (variant created from About page). Phase 2.5 Part B can proceed for Privacy/Terms once Jim confirms `hero-interior_about` is the canonical interior-page hero name to use.

### P0 — pre-ad launch (Jim-manual)
- [ ] Form → email delivery verification (carried P0)
- [ ] Replace 3 Gallery Photos CMS asset images with real deck photos from `website/pages/gallery/project-photos`

### P1 — cleanup (Jim-manual)
- [ ] Create `cta-final` Component Properties (headline, cta_label, cta_href)
- [ ] Terms page: delete empty H1 in rich-text body + fix SEO page title
- [ ] About page: delete placeholder "Heading 1" in rich-text body
- [ ] About hero-interior: optional promote inline section → component instance

### P2/P3 — content (Jim-manual, "later")
- [ ] Social URLs for Facebook / Instagram / Nextdoor
- [ ] Nextdoor business account verification status

### Queued for next session's web-dev dispatch
- [ ] **Micro-task:** restructure `hero-interior` so `bg_image` actually binds to an image component (currently styling-panel background, nothing to bind)
- [ ] **Phase 2.5 Part B:** nuke embedded hero from `header` component site-wide; add `hero-interior_about` variant to Privacy + Terms with hardcoded legal-page titles; final global verification across 21 pages
- [ ] Decision + execution on whether `hero-interior_about` becomes the canonical interior-page variant (single component) or stays separate

## Agent Activity

- **7 web-dev subagent dispatches** (all ran cleanly, some in parallel):
  1. Verification + Step 6 (serial — caught Wausau FAIL, proceeded per Jim's call)
  2–4. Steps 8/9/10 in parallel (builders-strip / coverage-map / cta-final)
  5–6. Step 7 + Phase 2.5 Part A in parallel (gallery-featured / hero-interior)
  7. Step 11 (homepage assembly)
  8. Step 11.5 (FAQ add)
- **Total components built in Webflow this session:** 7 (process-steps-v2, gallery-featured, builders-strip, coverage-map, cta-final, hero-interior, faq-section-home)
- **Pages touched in Webflow:** homepage (new spine + 13 sections removed), Service Areas Template (hero-interior inserted, propagates to 5 city pages), About (hero-interior inline section added)
- **MCP read-only calls:** CMS queries (Gallery Photos, FAQs, Service Areas), page inspections, Playwright verification snapshots across ~10 staging URLs
- **Production:** zero writes. All work on Webflow staging subdomain (`central-wisconsin-deck-builders.webflow.io`), `customDomains: []` on every publish call

## Memory Updates

Three new memory files created, one expanded:

1. **`webflow-mcp-pseudo-elements.md`** — Created early session; expanded twice through the day. Final strip list: `::before`/`::after`/`:hover`/`@keyframes`/compound pseudo-stacks/attribute selectors. Plus: inline `<script>` tags get quotes HTML-encoded, breaking execution. Workarounds: `style_tool` with `pseudo` param, real aria-hidden divs, inline site scripts with `!important`.
2. **`webflow-mcp-parallel-agents.md`** — Designer page context is shared state across concurrent agents. Re-select target page before structural edits.
3. **`webflow-mcp-sibling-insert.md`** — Body-level sibling insert adjacent to component instances can fail ("Cannot insert elements directly into a component instance"); workaround is remove + re-append trailing sections in correct order. Noted as state-dependent (didn't reproduce in Step 11.5).

All three linked from `MEMORY.md` index.

## Next Session

When Jim returns with Phase 2.5 Part B unblocked:

1. **Micro-task first:** restructure `hero-interior` so `bg_image` binds to a real image component (Jim flagged the current styling-panel-background approach isn't bindable). Web-dev agent will refactor the component's DOM so the background image is a proper `<img>` or Webflow image element that can accept a Component Property + CMS binding.
2. **Confirm canonical name:** decide whether Jim's `hero-interior_about` variant replaces the original `hero-interior` or stays separate. Consolidate if duplicative.
3. **Phase 2.5 Part B dispatch:** nuke embedded hero from `header` component site-wide; add interior hero variant to Privacy + Terms with hardcoded legal-page titles; global verification snapshot across all 21 pages (H1 counts, hero copy, mobile rendering).
4. **If gallery photos landed:** verify the 3 replacement images render correctly on homepage; update alt text to match actual photo content.
5. **If form-email verification passed:** re-date `/marketing/launch-brief-2026-04-20.md` to the post-revamp launch date and kick off ad-launch readiness review.
6. **Outstanding P1 cleanup (if Jim hasn't done them):** cta-final Component Properties + Terms/About rich-text debris.

Revamp timeline: day 2 of 11–15 complete. Pace: **well ahead of estimate** — Phase 2 shipped in a single day (originally budgeted 4–6 days).


