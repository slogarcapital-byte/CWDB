---
type: session
session-id: "2026-07-17-001"
agents-used: []
topics: [jobtread, hubspot, warehouse, test-cleanup]
decisions-made:
  - "Task 7 real-device acceptance gate: PASSED (intake row 7 round-trip clean)"
  - "Delete all test leads across JobTread + HubSpot + warehouse (Jim directive); bronze test rows deleted rather than retained"
  - "Test protocol addendum: HubSpot drops @cwdb-internal.test emails, clean HubSpot by name"
files-changed:
  - "_vault/sessions/2026-07-17-001.md"
  - "~/.claude/projects/.../memory/jobtread-hybrid.md"
previous-session: "[[2026-07-15-001]]"
next-session: ""
duration-minutes: 0
tags:
  - type/session
  - session/2026-07
created: 2026-07-17
updated: 2026-07-17
status: in-progress
---

# Session 2026-07-17-001

## Context
JobTread intake verification + full test-data cleanup. Note: created manually (SessionStart hook still broken, see 2026-07-15 open items).

## Work Done

- **Real-device acceptance PASSED (Task 7 closed):** Jim's iPhone re-test (intake row 7) round-tripped clean: JobTread account/contact "Jim testcase" with consent fields, Job in New Lead with correct labels; HubSpot 200; bronze row green.
- **JobTread new-lead notification CONFIRMED:** Jim built the automation in JobTread and received the notification on the test job.
- **All test leads deleted across all systems:**
  - JobTread: all 4 accounts removed child-first (Example Customer + Job 1, Example Vendor, Gateway Smoke Test, Jim testcase + 2 jobs). Org now has 0 accounts: clean slate for production.
  - HubSpot: 7 test contacts archived via CRM API (4x Gateway Smoke Test, Vocab Fix Test, 2x Jim testcase incl. the June-era dcebighitta12@aim.com contact).
  - Warehouse: fact_leads row 47 (June self-test) + 7 raw_intake_events + all raw_jobtread_events + outbox row + 13 raw_jobtread_snapshot rows deleted. Verified 0 residue.

## Decisions Made

- Bronze test rows deleted rather than retained: clean slate before production traffic (test round-trips are documented in session notes + commits instead).

## Open Items

- **DISCOVERY: HubSpot silently drops `@cwdb-internal.test` emails** (reserved TLD): the contact is created with NO email. Consequence: the v_clean_leads email exclusion never sees these contacts if they get pulled, and HubSpot cleanup must search by NAME, not email. Test protocol stays @cwdb-internal.test (warehouse bronze keeps the email); just remember the HubSpot side is name-only.
- Remaining JobTread plan items unchanged: [JIM] Google Ads conversion action + GA4 MP secret (Task 9), proposal templates with R4/R7 confirm-on-build gates + QBO sandbox-first sync (Task 10). Cron 7-day green watch continues.
- Hookify plugin hooks still broken (SessionStart absent).

## Agent Activity

None dispatched. Direct execution: Supabase MCP (SQL verify + purge), Pave API via PowerShell (JobTread reads + child-first deletes), HubSpot MCP (contact search) + HubSpot CRM API (archive; the MCP has no delete surface, used HUBSPOT_PRIVATE_APP_TOKEN from .env.local).

## Memory Updates

- `jobtread-hybrid.md`: added the HubSpot .test-email drop quirk (test contacts are email-less in HubSpot; clean by name; @cwdb-internal.test remains the protocol since bronze retains the email).

## Next Session

- Verify tomorrow's daily cron stays green with the emptied JobTread org (pull should write 0 snapshot rows without erroring); continue the 7-day green watch.
- Work Jim's remaining manual items when he's ready: Google Ads conversion action + env var, GA4 MP secret + Edge Function secrets (Task 9); proposal templates with R4/R7 confirm-on-build gates, QBO sandbox-first sync (Task 10).
- After Task 9 env vars land, run an end-to-end conversion test (new test lead -> Signed / Booked -> outbox -> worker upload -> Google Ads import), then clean up by name in HubSpot.
- Fix or reinstall the hookify plugin (SessionStart/Stop hooks still broken; session notes are being created manually).
